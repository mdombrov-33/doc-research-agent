# Use a fact-ledger-derived evaluation benchmark

The evaluation benchmark will use a fixed, reviewed synthetic corpus and evaluation cases
derived from an authoritative fact ledger. We chose this over production-derived data or
dynamically regenerated synthetic data because it makes expected evidence unambiguous, permits
deliberate edge cases, avoids user-data dependencies, and keeps regression results reproducible.
