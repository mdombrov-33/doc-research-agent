# ── Secrets (required) ──
# OpenAI — embeddings (text-embedding-3-small) only.
# The eval CI needs this too, for embeddings: ingesting the corpus + the separation check.
OPENAI_API_KEY=your_openai_api_key_here
# OpenRouter — all chat/LLM calls (the agent's reasoning + answer, grading, and the eval judges).
# Guardrails run locally (llm-guard) and need no API key.
OPENROUTER_API_KEY=your_openrouter_api_key_here

# ── Qdrant (required) ──
# QDRANT_MODE is "local" or "cloud"; cloud needs the URL + API key.
QDRANT_MODE=cloud
QDRANT_LOCAL_URL=http://localhost:6333
QDRANT_CLOUD_URL=https://your-cluster.qdrant.io
QDRANT_API_KEY=your_qdrant_api_key_here
QDRANT_COLLECTION_NAME=documents
# QDRANT_QUERY_TIMEOUT_SECONDS=10
# QDRANT_INGESTION_TIMEOUT_SECONDS=30

# ── App (required) ──
APP_ENV=development
LOG_LEVEL=INFO
# Where the Streamlit UI reaches the backend.
API_URL=http://localhost:8000

# ── Optional overrides (defaults live in src/config.py) ──
# LLM_MODEL=anthropic/claude-sonnet-4.6
# PLANNER_MODEL=openai/gpt-5.6-luna
# ASSESSOR_MODEL=openai/gpt-5.6-luna
# PLANNER_MAX_TOKENS=256
# ASSESSOR_MAX_TOKENS=256
# ANSWER_MAX_TOKENS=1000
# LLM_MAX_RETRIES=3
# LLM_TIMEOUT_SECONDS=60
# QUERY_TIMEOUT_SECONDS=1000
# CONVERSATION_HISTORY_TURNS=3
# WEB_SEARCH_TIMEOUT_SECONDS=30
# EMBEDDING_MODEL=text-embedding-3-small
# EMBEDDING_DIMENSION=1536
# EMBEDDING_TIMEOUT_SECONDS=30
# EMBEDDING_MAX_RETRIES=2
# UPLOAD_DIR=./uploads
# MAX_UPLOAD_BYTES=31457280
# UPLOAD_READ_CHUNK_BYTES=1048576
# MAX_PDF_PAGES=1000
# MAX_EXTRACTED_CHARACTERS=3000000
# MAX_CHUNKS_PER_DOCUMENT=3500
# DATA_DIR=./data   # home for local SQLite telemetry and checkpoint databases
# CHECKPOINT_BACKEND=sqlite  # optional: postgres with DATABASE_URL
# METRICS_BACKEND=sqlite  # optional: postgres with DATABASE_URL
# DATABASE_URL=postgresql://user:password@host:5432/database
# Answer cache: replay repeated first-turn document answers from Qdrant
# ANSWER_CACHE_ENABLED=true
# ANSWER_CACHE_COLLECTION=answer_cache
# Internal retrieval budgets: one or two branches merge globally, then rerank once.
# RETRIEVAL_CANDIDATE_BUDGET=40
# RETRIEVAL_EVIDENCE_BUDGET=8
# RERANK_MODEL=Xenova/ms-marco-MiniLM-L-6-v2
# Minimum cross-encoder score (raw logit) a chunk must clear to enter the evidence pool.
# Calibrated on the golden set via `make eval-rerank-sweep` (drops ~99% of labelled-irrelevant
# chunks at zero recall cost). Code default is unset (no floor); this ships it as the deployed
# default. Recalibrate if the reranker model changes.
RERANK_SCORE_FLOOR=-9.0
# Per-IP rate limiting on /stream and /upload (slowapi)
# RATE_LIMIT_ENABLED=true
# RATE_LIMIT=30/minute
# RATE_LIMIT_STORAGE_URI=memory:// 

# ── Build metadata (CI/Docker) ──
# Stamped on every log line. Baked into the image via --build-arg (see Dockerfile).
# Unset locally is fine — logs show git_sha=unknown / version=unknown.
# GIT_SHA=
# APP_VERSION=
